# Local development requirements (macOS / Python 3.11)
#
# Trimmed from requirements.txt to only the packages this project actually
# imports. The original requirements.txt is UTF-16 encoded and carries
# Windows-only and unused entries (windows-curses, virtualenvwrapper-win,
# PyAutoGUI, Flask, psycopg2, weasyprint, ...) that fail or bloat on macOS.
#
# Install with:  .venv/bin/pip install -r requirements-local.txt

# --- Framework ---
Django==4.1.3

# --- Django apps listed in INSTALLED_APPS ---
django-autoslug==1.9.9
django-ckeditor-5==0.2.18
django-model-utils==4.3.1
django-private-storage==3.1.1
django-wkhtmltopdf==3.4.0

# --- Document / report generation (taxes app) ---
python-docx==1.1.0
docxtpl==0.16.7
pypdf==3.17.4
reportlab==4.0.9
xlwt==1.3.0

# --- Image handling (ImageField / Pillow) ---
Pillow==9.5.0

# --- MySQL driver ---
# Bumped from the pinned 2.1.1, which does not build against the client
# libraries of the Homebrew MySQL installed here (26.7.0).
mysqlclient==2.2.7

# --- Transitive runtime dependency ---
# django-model-utils 4.3.1 imports pkg_resources at import time, which modern
# virtualenvs no longer provide by default. Capped below 81, where setuptools
# stops shipping pkg_resources.
setuptools<81
